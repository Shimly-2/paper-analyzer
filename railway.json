{
  "build": {
    "builder": "NIXPACKS",
    "nixpacks": {
      "providers": ["node", "python"],
      "phases": {
        "setup": {
          "nixPkgs": ["python311", "python311Packages.pip"],
          "commands": ["pip install -r requirements.txt"],
          "nodeCommands": ["npm install"]
        }
      }
    }
  },
  "deploy": {
    "startCommand": "echo 'Node+Python env installed successfully'"
  }
}
